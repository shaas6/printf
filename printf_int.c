#include "holberton.h"

/**
 * printf_int - Print integers; both decimal and whole
 * @n: Number
 * Return: Number
 */

int printf_int(va_list arg)
{
	int n = va_arg(arg, int);

	itoa
   return (0);
}
